if(IS_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/linked-list")
  add_subdirectory("linked-list")
endif()

if(IS_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/array-list")
  add_subdirectory("array-list")
endif()
